<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
  <link rel="stylesheet" href="/style.css">
  <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</head>

  <div class="wrapper">
    <header>
        <h1 class="has-text-weight-bold">Tech Blog</h1>
        <nav class="navbar mt-2">
            {{#if loggedIn}}
            <a href="/" id="navBtn" class="button is-outlined is-rounded">Homepage</a>
            <a href="/dashboard" id="navBtn" class="button is-outlined is-rounded">Your Dashboard</a>
            <a href="/dashboard/create/" id="navBtn" class="button is-outlined is-rounded">New Post</a>
            <button id="logout" class="button is-danger is-outlined is-rounded">Logout</button>
            {{else}}
            <a href="/" id="navBtn" class="button is-outlined is-rounded">Homepage</a>
            <a href="/login" id="" class="button is-hoverable is-outlined is-rounded">Login</a>
            {{/if}}
        </nav>
    </header>

    <main>
            {{{ body }}}
    </main>

    

{{#if loggedIn}}
<script src="/logout.js"></script>
{{/if}}
</body>

</html>