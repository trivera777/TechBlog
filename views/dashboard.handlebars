    <div class="mb-5 pt-3">
        <a href="/dashboard/create/">
        <button id="createBtn" class="button is-outlined is-rounded is-pulled-right">Create Post</button></a>
        
        <h2>Dashboard</h2><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}">
        </div>
            <button id="editBtn" class="button is-outlined is-rounded">edit post</button></a>
    {{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">you do not currently have any posts</h6>

{{/if}}


